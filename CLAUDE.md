# AI 에이전트용 작업 규칙

> 각자 쓰는 AI 도구(Claude Code, Cursor 등)에 넣어둘 규칙 모음.
> **권장: 각 레포 루트에 `CLAUDE.md` 파일로 커밋**해두면 붙여넣기 없이 모두의 AI가 자동으로 읽음 (Cursor는 `.cursorrules`, 범용은 `AGENTS.md`).
> 사용법: 아래 [공통 규칙] + [자기가 작업하는 레포 블록] + [자기 소유권 블록]을 합쳐서 넣는다.

---

## 공통 규칙 (전원, 모든 레포)

```markdown
# Git 작업 규칙

## 절대 금지
- main, dev 브랜치에 직접 커밋/push 금지. 반드시 dev에서 기능 브랜치를 만들어 작업한다.
- main은 출시/배포용 — dev → main 머지는 배포 담당만 한다. main으로 PR을 만들지 않는다.
- force push (`push -f`), `reset --hard origin/*` 금지. 필요해 보여도 멈추고 사용자에게 확인받는다.
- `.env` 등 시크릿 파일을 커밋하지 않는다. 새 환경변수가 생기면 `.env.example`에 키만 추가하고, 사용자에게 "팀 단톡에 공지하라"고 알린다.
- 아래 "내 소유 영역" 밖의 파일은 수정하지 않는다. 수정이 꼭 필요하면 먼저 사용자에게 "이 파일은 ○○ 소유라서 공지/리뷰가 필요하다"고 경고한다.

## 작업 시작
- 작업 시작 전 반드시: `git switch dev && git pull` 후 새 브랜치 생성.
- 브랜치 이름: `타입/영역-설명` (예: feat/home-지역카드, fix/ocr-금액인식).
  타입: feat | fix | refactor | docs | style | test | chore

## 커밋
- 커밋 메시지 형식: `타입(영역): 요약` — 한글, 50자 이내, 명령형.
  예: feat(mytrip): 인증 체크리스트 진행률 표시 추가
- 논리 단위로 작게 자주 커밋한다. 여러 기능을 한 커밋에 섞지 않는다.
- 커밋 메시지에 AI 공동작성자 표기를 넣지 않는다. `Co-Authored-By: Claude ...`, `🤖 Generated with ...` 등 AI가 작성했음을 드러내는 트레일러·문구를 절대 추가하지 않는다 (작성자는 본인 단독으로 남긴다).

## 브랜치 전략
- 브랜치 계층: `main ← dev ← backend | frontend ← feat/*`
- `backend`, `frontend`는 파트별 통합 브랜치다. 기능 브랜치(feat/*, fix/* 등)는 자기 파트 통합 브랜치에서 분기하고, 같은 통합 브랜치로 머지한다.
  - 백엔드 작업: `backend`에서 분기 → 작업 → `backend`로 머지
  - 프론트 작업: `frontend`에서 분기 → 작업 → `frontend`로 머지
- 기능이 안정되면 `backend`/`frontend` → `dev`로 머지한다 (마일스톤 단위).
- 로컬에서 직접 머지할 때는 `--no-ff`로 머지해 브랜치 이력을 남긴다.

## PR / 머지
- PR은 항상 base를 dev로 만든다 (main 아님). 머지는 Squash merge. PR 제목은 커밋 메시지와 같은 형식.
- 승인 대기 없이 셀프 머지 가능. 단 공유 영역 파일을 건드린 PR이면 머지 전에 사용자에게 "단톡 공지 먼저 하라"고 알린다.
- 한 PR은 변경 300줄 이내를 목표로 한다. 넘을 것 같으면 작업을 쪼개자고 제안한다.
- 한 브랜치가 3일 넘게 살아있으면 사용자에게 "지금까지 한 것만 먼저 PR로 올리자"고 제안한다.
- 머지 후에는 dev를 pull하고 로컬 브랜치를 삭제한다.

